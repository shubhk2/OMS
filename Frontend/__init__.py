'''for other employees,our current index.html is perfect,but for employees with role(id)=14(Admin),1st page will look a bit different.it will not contain the checkin-out timer instead it will contain the latest checked in employees with an arrow on which if clicked it takes them to /admin/attendence(on dashboarcd it will be opened using "Attendence" tab only.Instead of Primary Team card there will be a Explore Project card which will list 4 on going project and an arrow which will take him to /admin/projects.
Also instead of Welcome,User we want to show the name of the user(till the first space(basically first name))'''